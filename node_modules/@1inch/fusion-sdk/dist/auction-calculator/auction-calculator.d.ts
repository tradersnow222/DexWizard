import { SettlementPostInteractionData } from '../fusion-order/settlement-post-interaction-data';
import { AuctionDetails, AuctionPoint } from '../fusion-order/auction-details';
export declare class AuctionCalculator {
    private readonly startTime;
    private readonly duration;
    private readonly initialRateBump;
    private readonly points;
    private readonly takerFeeRatio;
    constructor(startTime: bigint, duration: bigint, initialRateBump: bigint, points: AuctionPoint[], takerFeeRatio: bigint);
    static fromAuctionData(data: SettlementPostInteractionData, details: AuctionDetails): AuctionCalculator;
    calcAuctionTakingAmount(takingAmount: string, rate: number): string;
    calcRateBump(time: number): number;
}
